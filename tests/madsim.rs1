use std::time::{Duration, Instant};

use futures_timer::Delay;

#[madsim::test]
async fn delay_on_madsim() {
    let i = Instant::now();
    let dur = Duration::from_millis(100);
    let _d = Delay::new(dur).await;
    assert!(i.elapsed() > dur);
}
